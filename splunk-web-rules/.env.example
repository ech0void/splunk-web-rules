# Splunk REST API — copy to .env and fill in your values
# NEVER commit the real .env to Git!

SPLUNK_HOST=https://your-splunk-server:8089
SPLUNK_TOKEN=your-splunk-api-token
SPLUNK_APP=search
SPLUNK_VERIFY_SSL=false
